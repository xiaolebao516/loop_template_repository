# Agent Loop Engineering Initialization Prompt

This source-repository prompt is not deployed as a runtime file. The deployed `AGENTS.md` owns the initialization rules.

Send the Agent exactly:

> 初始化 Agent Loop Engineering。
